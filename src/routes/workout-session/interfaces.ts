export enum WorkoutState {
  WARMUP = 'warmup',
  EXERCISE = 'exercise',
  REST = 'rest',
  FINISHED = 'finished',
}

export enum ExerciseState {
  IDLE = 'idle',
  PAUSED = 'paused',
  STARTED = 'started',
  COMPLETED = 'completed',
}
